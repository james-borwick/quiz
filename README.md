# Quiz

* Lists out JSON files within the *main* directory
* Selects a JSON file at random
* Selects a random question from the JSON file map and displays it to the user
* Keeps track of correct and incorrect answers
* Displays the results when user quits
